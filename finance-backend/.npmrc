# Ensure devDependencies install on hosts that set NODE_ENV=production during `npm install`
# (helps local tools; `@types/*` are also re-installed in the `build` script before `tsc`).
production=false
